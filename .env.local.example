# ─── Database ─────────────────────────────────────────────────────────
# PostgreSQL connection string
DATABASE_URL="postgresql://postgres.xtteftuhcnvkbafmrjba:tataTATA123@@@@aws-1-eu-central-1.pooler.supabase.com:5432/postgres"

# ─── Auth ─────────────────────────────────────────────────────────────
# Change this to a long random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-secret-key-minimum-32-characters-long-change-in-production"

# Optional: override default admin credentials when DB is unavailable
ADMIN_EMAIL="fnigus33@gmail.com"
ADMIN_PASSWORD="tataTATA123"

# ─── Stripe (optional) ────────────────────────────────────────────────
STRIPE_PUBLIC_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# ─── App ──────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# ─── Email (SMTP via Nodemailer) ────────────────────────────────────
# Use any SMTP provider: Gmail, SendGrid, Mailgun, Resend SMTP, etc.
# Example for Gmail app password:
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="notifications@marklandhotel.com"
SMTP_PASS="your-app-password"
SMTP_FROM="Markland Hotel & Spa <notifications@marklandhotel.com>"
ADMIN_NOTIFY_EMAIL="manager@marklandhotel.com"

# Optional: Disable email in development
SEND_EMAILS="true"
